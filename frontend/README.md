# BLOK Frontend

Vite + React SPA for BLOK. Deploy to Cloudflare Pages.

```bash
npm install
cp .env.example .env
npm run dev
```

Build:

```bash
npm run build
```

`public/_redirects` enables SPA routing on Pages (`/* → /index.html`).